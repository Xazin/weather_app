import 'package:flutter/material.dart';

class CurrentForecastScreen extends StatelessWidget {
  const CurrentForecastScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Placeholder(),
    );
  }
}
